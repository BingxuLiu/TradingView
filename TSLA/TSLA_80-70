//@version=6
strategy('温度计', initial_capital = 10000, default_qty_value = 100, default_qty_type = strategy.percent_of_equity, overlay = false, calc_on_every_tick = true, margin_long = 50)


//要在图标中引入二二温度计，在本策略中选择数据源为二二温度计：温度计，如果没有生效就多选几次，等他重新生成回测报告
finalScore = input.source(title="二二温度计", defval=close)

// 绘制
plot(finalScore, title = 'MOScore', color = color.gray)

if finalScore > 80 and strategy.position_size == 0
    strategy.entry("BUY", strategy.long, comment="买入")
if finalScore < 70 and strategy.position_size > 0
    strategy.close("BUY", comment="卖出")